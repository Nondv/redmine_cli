% list = RedmineRest::Models::IssueStatus.all
%
% old_status = list.find { |i| i.id == old_value }
% old = if old_status
%         "#{old_status.name}[#{old_status.id}]"
%       else
%         'nil'
%       end
%
% new_status = list.find { |i| i.id == new_value }
% new = if new_status
%         "#{new_status.name}[#{new_status.id}]"
%       else
%         'nil'
%       end
%
% output = old + '  ->  '.white + new
<%= output %>
